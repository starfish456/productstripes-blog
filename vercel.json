{
  "buildCommand": "npm ci --include=dev && npx convex deploy --cmd 'npm run build'",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/proxy"
    }
  ]
}
